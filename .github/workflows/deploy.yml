name: Deploy to REG.RU Hosting

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare SSH key (base64)
        run: |
          echo "Длина base64 ключа: $(echo "${{ secrets.VPS_SSH_KEY_B64 }}" | wc -c)"
          echo "${{ secrets.VPS_SSH_KEY_B64 }}" | base64 -d > key.pem
          chmod 600 key.pem
          echo "Размер декодированного ключа: $(wc -c < key.pem)"
          echo "Начало ключа:"
          head -1 key.pem
          echo "Конец ключа:"
          tail -1 key.pem

      - name: Add host to known_hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy over SSH
        env:
          SSH_PORT: ${{ secrets.VPS_SSH_PORT }}
          VPS_PATH: ${{ secrets.VPS_PATH }}
        run: |
          PORT_OPT=${SSH_PORT:+-p $SSH_PORT}
          echo "🔍 SSH параметры:"
          echo "  Host: ${{ secrets.VPS_HOST }}"
          echo "  User: ${{ secrets.VPS_USER }}"
          echo "  Port: ${{ secrets.VPS_SSH_PORT }}"
          echo "  Path: ${{ secrets.VPS_PATH }}"
          echo "🔍 Тестируем SSH соединение..."
          echo "Проверяем доступность сервера..."
          ping -c 3 ${{ secrets.VPS_HOST }} || echo "Ping не работает"
          
          # Проверяем SSH соединение на порту 22
          echo "Проверяем SSH соединение на порту 22..."
          if timeout 30 ssh -i key.pem -o ConnectTimeout=20 -o ServerAliveInterval=10 -o ServerAliveCountMax=3 -o StrictHostKeyChecking=no -o LogLevel=DEBUG3 ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} "whoami" 2>&1; then
            echo "✅ SSH работает на порту 22"
            export WORKING_SSH_PORT=22
          else
            echo "❌ SSH не работает на порту 22"
            exit 1
          fi
          echo "🔍 Начинаем деплой через SSH на порту $WORKING_SSH_PORT..."
          echo "Проверяем стабильность соединения..."
          ssh -i key.pem -p $WORKING_SSH_PORT -o ConnectTimeout=60 -o ServerAliveInterval=15 -o ServerAliveCountMax=3 -o StrictHostKeyChecking=no ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} "whoami"
          
          echo "🔍 Начинаем основной деплой..."
          ssh -i key.pem -p $WORKING_SSH_PORT -o ConnectTimeout=60 -o ServerAliveInterval=15 -o ServerAliveCountMax=3 -o StrictHostKeyChecking=no ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} "
            set +e  # Не прерывать выполнение при ошибках
            echo '=== Начинаем деплой на REG.RU хостинг ==='
            echo 'Пользователь: \$(whoami)'
            echo 'UID: \$(id -u)'
            echo 'Текущая директория: \$(pwd)'
            
            # Создать директорию проекта
            mkdir -p \"$VPS_PATH\"
            cd \"$VPS_PATH\"
            echo 'Рабочая директория: \$(pwd)'
            
            # Установить зависимости в пользовательскую директорию
            echo '=== Устанавливаем зависимости ==='
            pip3 install --user uvicorn fastapi flask requests
            
            # Клонировать или обновить код
            if [ ! -d \".git\" ]; then
              echo '=== Клонируем репозиторий ==='
              # Если директория не пустая, создаем временную папку
              if [ \"\$(ls -A .)\" ]; then
                echo 'Директория не пустая, создаем временную папку'
                mkdir -p temp_repo
                cd temp_repo
                git clone https://github.com/${{ github.repository }} .
                # Копируем файлы обратно
                cp -r * ../
                cp -r .git ../
                cd ..
                rm -rf temp_repo
              else
                git clone https://github.com/${{ github.repository }} .
              fi
            else
              echo '=== Обновляем код ==='
              git fetch --all
              git reset --hard origin/main
            fi
            
            # Проверить зависимости
            echo '=== Проверяем зависимости ==='
            python3 -c 'import uvicorn, fastapi, flask; print(\"Зависимости OK\")' || echo 'Зависимости НЕ УСТАНОВЛЕНЫ'
            
            # Проверить структуру проекта
            echo '=== Проверяем структуру проекта ==='
            ls -la
            if [ -d \"backend/\" ]; then
              echo 'backend/ найден'
              ls -la backend/
              if [ -d \"backend/app/\" ]; then
                echo 'backend/app/ найден'
                ls -la backend/app/
              else
                echo 'backend/app/ не найден'
              fi
            else
              echo 'backend/ не найден - попробуем создать структуру'
              mkdir -p backend/app
              echo 'Создана структура backend/app/'
            fi
            
            # Остановить старые процессы
            echo '=== Останавливаем старые процессы ==='
            echo 'Ищем процессы uvicorn...'
            ps aux | grep uvicorn | grep -v grep || echo 'uvicorn процессы не найдены'
            echo 'Ищем процессы backend_simple...'
            ps aux | grep backend_simple | grep -v grep || echo 'backend_simple процессы не найдены'
            
            echo 'Останавливаем uvicorn процессы...'
            pkill -f 'uvicorn.*simple_main' 2>/dev/null || echo 'uvicorn процессы не найдены для остановки'
            echo 'Останавливаем backend_simple процессы...'
            pkill -f 'backend_simple.py' 2>/dev/null || echo 'backend_simple процессы не найдены для остановки'
            
            echo 'Ждем завершения процессов...'
            sleep 3
            
            echo 'Проверяем, что процессы остановлены...'
            ps aux | grep -E '(uvicorn|backend_simple)' | grep -v grep || echo 'Все процессы остановлены'
            
            # Запустить FastAPI (будет доступен через Nginx)
            echo '=== Запускаем FastAPI ==='
            echo 'Проверяем порт 8080...'
            netstat -tlnp | grep :8080 || echo 'Порт 8080 свободен'
            echo 'Проверяем порт 5000...'
            netstat -tlnp | grep :5000 || echo 'Порт 5000 свободен'
            
            cd backend
            echo 'Запускаем FastAPI на порту 8080...'
            nohup python3 -m uvicorn app.simple_main:app --host 127.0.0.1 --port 8080 > /tmp/fastapi.log 2>&1 &
            FASTAPI_PID=\$!
            echo \"FastAPI запущен с PID: \$FASTAPI_PID на порту 8080\"
            sleep 3
            
            # Запустить Server Overpass API
            echo '=== Запускаем Server Overpass API ==='
            cd ..
            echo 'Запускаем Server Overpass API на порту 5000...'
            nohup python3 backend_simple.py > /tmp/overpass.log 2>&1 &
            OVERPASS_PID=\$!
            echo \"Server Overpass API запущен с PID: \$OVERPASS_PID на порту 5000\"
            sleep 3
            
            # Проверить работу сервисов
            echo '=== Проверяем работу сервисов ==='
            echo 'Процессы Python:'
            ps aux | grep python | grep -v grep || echo 'Python процессы не найдены'
            
            echo '=== Проверяем FastAPI ==='
            curl -s http://127.0.0.1:8080/health || echo 'FastAPI не отвечает'
            
            echo '=== Проверяем Server Overpass API ==='
            curl -s http://127.0.0.1:5000/api/health || echo 'Server Overpass API не отвечает'
            
            echo '=== Логи FastAPI ==='
            cat /tmp/fastapi.log || echo 'FastAPI лог пуст'
            
            echo '=== Логи Server Overpass API ==='
            cat /tmp/overpass.log || echo 'Server Overpass API лог пуст'
            
            echo \"Deployed FastAPI at: http://${{ secrets.VPS_HOST }}:8080/health\"
            echo \"Deployed Server Overpass API at: http://${{ secrets.VPS_HOST }}:5000/api/health\"
            echo \"TrailSpot website: http://trailspot.app\"
            echo \"TrailSpot API: http://trailspot.app/api/health\"
          "