name: Deploy to REG.RU Hosting

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare SSH key (base64)
        run: |
          echo "Длина base64 ключа: $(echo "${{ secrets.VPS_SSH_KEY_B64 }}" | wc -c)"
          echo "${{ secrets.VPS_SSH_KEY_B64 }}" | base64 -d > key.pem
          chmod 600 key.pem
          echo "Размер декодированного ключа: $(wc -c < key.pem)"
          echo "Начало ключа:"
          head -1 key.pem
          echo "Конец ключа:"
          tail -1 key.pem

      - name: Add host to known_hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts

      - name: Deploy over SSH
        env:
          SSH_PORT: ${{ secrets.VPS_SSH_PORT }}
          VPS_PATH: ${{ secrets.VPS_PATH }}
        run: |
          PORT_OPT=${SSH_PORT:+-p $SSH_PORT}
          echo "🔍 SSH параметры:"
          echo "  Host: ${{ secrets.VPS_HOST }}"
          echo "  User: ${{ secrets.VPS_USER }}"
          echo "  Port: ${{ secrets.VPS_SSH_PORT }}"
          echo "  Path: ${{ secrets.VPS_PATH }}"
          echo "🔍 IP адрес GitHub Actions:"
          curl -s https://ipinfo.io/ip || echo "Не удалось получить IP"
          echo "🔍 Начинаем деплой через SSH..."
          echo "Проверяем доступность сервера..."
          ping -c 3 ${{ secrets.VPS_HOST }} || echo "Ping не работает"
          
          echo "🔍 Выполняем весь деплой в одном SSH сеансе..."
          ssh -i key.pem -o ConnectTimeout=60 -o ServerAliveInterval=15 -o ServerAliveCountMax=3 -o StrictHostKeyChecking=no ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} "
            set +e  # Не прерывать выполнение при ошибках
            echo '=== Начинаем деплой на REG.RU хостинг ==='
            echo 'Пользователь: \$(whoami)'
            echo 'UID: \$(id -u)'
            echo 'Текущая директория: \$(pwd)'
            
            # Создать директорию проекта
            mkdir -p \"$VPS_PATH\"
            cd \"$VPS_PATH\"
            echo 'Рабочая директория: \$(pwd)'
            
            # Установить зависимости в пользовательскую директорию
            echo '=== Устанавливаем зависимости ==='
            pip3 install --user uvicorn fastapi flask requests
            
            # Клонировать или обновить код
            if [ ! -d \".git\" ]; then
              echo '=== Клонируем репозиторий ==='
              # Если директория не пустая, создаем временную папку
              if [ \"\$(ls -A .)\" ]; then
                echo 'Директория не пустая, создаем временную папку'
                mkdir -p temp_repo
                cd temp_repo
                git clone https://github.com/${{ github.repository }} .
                # Копируем файлы обратно
                cp -r * ../
                cp -r .git ../
                cd ..
                rm -rf temp_repo
              else
                git clone https://github.com/${{ github.repository }} .
              fi
            else
            echo '=== Обновляем код ==='
            git fetch --all
            git reset --hard origin/main
            fi
            
            # Проверить зависимости
            echo '=== Проверяем зависимости ==='
            python3 -c 'import uvicorn, fastapi, flask; print(\"Зависимости OK\")' || echo 'Зависимости НЕ УСТАНОВЛЕНЫ'
            
            # Проверить структуру проекта
            echo '=== Проверяем структуру проекта ==='
            ls -la
            if [ -d \"backend/\" ]; then
              echo 'backend/ найден'
            ls -la backend/
              if [ -d \"backend/app/\" ]; then
                echo 'backend/app/ найден'
            ls -la backend/app/
              else
                echo 'backend/app/ не найден'
              fi
            else
              echo 'backend/ не найден - попробуем создать структуру'
              mkdir -p backend/app
              echo 'Создана структура backend/app/'
            fi
            
            # Остановить старые процессы
            echo '=== Останавливаем старые процессы ==='
            echo 'Ищем процессы uvicorn...'
            ps aux | grep uvicorn | grep -v grep || echo 'uvicorn процессы не найдены'
            echo 'Ищем процессы backend_simple...'
            ps aux | grep backend_simple | grep -v grep || echo 'backend_simple процессы не найдены'
            
            echo 'Останавливаем uvicorn процессы...'
            pkill -f 'uvicorn.*simple_main' 2>/dev/null || echo 'uvicorn процессы не найдены для остановки'
            echo 'Останавливаем backend_simple процессы...'
            pkill -f 'backend_simple.py' 2>/dev/null || echo 'backend_simple процессы не найдены для остановки'
            
            echo 'Ждем завершения процессов...'
            sleep 3
            
            echo 'Проверяем, что процессы остановлены...'
            ps aux | grep -E '(uvicorn|backend_simple)' | grep -v grep || echo 'Все процессы остановлены'
            
            # Создаем Flask приложение для REG.RU (Passenger WSGI)
            echo '=== Создаем Flask приложение для REG.RU ==='
            
            # Создаем основной Flask файл с интегрированным Overpass API
            echo 'from flask import Flask, send_from_directory, jsonify, request' > hello.py
            echo 'import os' >> hello.py
            echo 'import requests' >> hello.py
            echo 'import json' >> hello.py
            echo '' >> hello.py
            echo 'application = Flask(__name__)' >> hello.py
            echo '' >> hello.py
            echo '# Overpass API URL' >> hello.py
            echo 'OVERPASS_URL = \"https://overpass-api.de/api/interpreter\"' >> hello.py
            echo '' >> hello.py
            echo '@application.route(\"/\")' >> hello.py
            echo 'def index():' >> hello.py
            echo '    return send_from_directory(\".\", \"index.html\")' >> hello.py
            echo '' >> hello.py
            echo '@application.route(\"/<path:filename>\")' >> hello.py
            echo 'def static_files(filename):' >> hello.py
            echo '    return send_from_directory(\".\", filename)' >> hello.py
            echo '' >> hello.py
            echo '@application.route(\"/api/health\")' >> hello.py
            echo 'def health():' >> hello.py
            echo '    return jsonify({\"status\": \"ok\", \"message\": \"TrailSpot API работает\"})' >> hello.py
            echo '' >> hello.py
            echo '@application.route(\"/api/overpass/paths\", methods=[\"POST\"])' >> hello.py
            echo 'def overpass_paths():' >> hello.py
            echo '    try:' >> hello.py
            echo '        data = request.get_json()' >> hello.py
            echo '        bbox = data.get(\"bbox\", \"\")' >> hello.py
            echo '        query = f\"[out:json][timeout:25];(way[highway=path][bbox:{bbox}];);out geom;\"' >> hello.py
            echo '        response = requests.post(OVERPASS_URL, data=query, timeout=30)' >> hello.py
            echo '        return jsonify(response.json())' >> hello.py
            echo '    except Exception as e:' >> hello.py
            echo '        return jsonify({\"error\": str(e)}), 500' >> hello.py
            echo '' >> hello.py
            echo '@application.route(\"/api/overpass/barriers\", methods=[\"POST\"])' >> hello.py
            echo 'def overpass_barriers():' >> hello.py
            echo '    try:' >> hello.py
            echo '        data = request.get_json()' >> hello.py
            echo '        bbox = data.get(\"bbox\", \"\")' >> hello.py
            echo '        query = f\"[out:json][timeout:25];(way[barrier][bbox:{bbox}];);out geom;\"' >> hello.py
            echo '        response = requests.post(OVERPASS_URL, data=query, timeout=30)' >> hello.py
            echo '        return jsonify(response.json())' >> hello.py
            echo '    except Exception as e:' >> hello.py
            echo '        return jsonify({\"error\": str(e)}), 500' >> hello.py
            echo '' >> hello.py
            echo 'if __name__ == \"__main__\":' >> hello.py
            echo '    application.run(host=\"0.0.0.0\")' >> hello.py

            # Создаем passenger_wsgi.py
            echo 'import sys' > passenger_wsgi.py
            echo 'import os' >> passenger_wsgi.py
            echo 'INTERP = os.path.expanduser(\"/var/www/u3288673/data/venv/bin/python\")' >> passenger_wsgi.py
            echo 'if sys.executable != INTERP:' >> passenger_wsgi.py
            echo '    os.execl(INTERP, INTERP, *sys.argv)' >> passenger_wsgi.py
            echo 'sys.path.append(os.getcwd())' >> passenger_wsgi.py
            echo 'from hello import application' >> passenger_wsgi.py

            echo 'Flask приложение создано для Passenger WSGI'
            
            # Создаем файл перезапуска для Passenger
            echo '=== Создаем файл перезапуска ==='
            touch .restart-app
            echo 'Файл .restart-app создан для перезапуска Passenger'
            
            # Проверить работу сервисов
            echo '=== Проверяем работу сервисов ==='
            echo 'Процессы Python:'
            ps aux | grep python | grep -v grep || echo 'Python процессы не найдены'
            
            echo '=== Проверяем работу Flask приложения ==='
            echo 'Проверяем файлы:'
            ls -la hello.py passenger_wsgi.py || echo 'Файлы не найдены'
            
            echo '=== Создаем файл перезапуска ==='
            touch .restart-app
            echo 'Файл .restart-app создан для перезапуска Passenger'
            
            echo \"TrailSpot website: http://trailspot.app\"
            echo \"TrailSpot API: http://trailspot.app/api/health\"
          "