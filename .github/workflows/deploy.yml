name: Deploy to VPS

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare SSH key (base64)
        run: |
          echo "${{ secrets.VPS_SSH_KEY_B64 }}" | base64 -d > key.pem
          chmod 600 key.pem

      - name: Add host to known_hosts
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H ${{ secrets.VPS_HOST }} >> ~/.ssh/known_hosts

      - name: SSH debug (verbose)
        env:
          SSH_PORT: ${{ secrets.VPS_SSH_PORT }}
        run: |
          PORT_OPT=${SSH_PORT:+-p $SSH_PORT}
          ssh -vvv -i key.pem -o StrictHostKeyChecking=no $PORT_OPT ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} "echo 'SSH OK'"

      - name: Deploy over SSH
        env:
          SSH_PORT: ${{ secrets.VPS_SSH_PORT }}
          VPS_PATH: ${{ secrets.VPS_PATH }}
        run: |
          PORT_OPT=${SSH_PORT:+-p $SSH_PORT}
          echo "🔍 SSH параметры:"
          echo "  Host: ${{ secrets.VPS_HOST }}"
          echo "  User: ${{ secrets.VPS_USER }}"
          echo "  Port: ${{ secrets.VPS_SSH_PORT }}"
          echo "  Key file exists: $(ls -la key.pem)"
          echo "🔍 Тестируем SSH соединение..."
          ssh -i key.pem $PORT_OPT ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} "echo 'SSH соединение работает!'"
          echo "🔍 Начинаем деплой через SSH..."
          ssh -i key.pem $PORT_OPT ${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }} "
            # Не используем set -e, чтобы команды с ошибками не прерывали скрипт
            echo '=== Начинаем деплой ==='
            echo 'Пользователь: \$(whoami)'
            echo 'UID: \$(id -u)'
            echo 'Права на /tmp: \$(ls -ld /tmp)'
            
            # подготовить каталог с кодом
            mkdir -p \"$VPS_PATH\"
            if [ ! -d \"$VPS_PATH/.git\" ]; then
              cd \"\$(dirname \"$VPS_PATH\")\"
              if [ -d rogein-generator ]; then rm -rf rogein-generator; fi
              git clone https://github.com/${{ github.repository }} rogein-generator
              mv rogein-generator \"\$(basename \"$VPS_PATH\")\"
            fi

            cd \"$VPS_PATH\"
            echo '=== Обновляем код ==='
            git fetch --all
            git reset --hard origin/main
            
            echo '=== Проверяем структуру файлов ==='
            ls -la
            ls -la backend/
            ls -la backend/app/
            echo '=== Проверяем фронтенд файлы ==='
            ls -la index.html || echo 'index.html не найден'
            ls -la src/ || echo 'src/ не найден'
            
            echo '=== Проверяем Python и зависимости ==='
            python3 --version
            python3 -c 'import uvicorn; print(\"uvicorn OK\")' || echo 'uvicorn НЕ УСТАНОВЛЕН'
            python3 -c 'import fastapi; print(\"fastapi OK\")' || echo 'fastapi НЕ УСТАНОВЛЕН'
            
            echo '=== Устанавливаем зависимости если нужно ==='
            if ! python3 -c 'import uvicorn' 2>/dev/null; then
              echo 'Устанавливаем uvicorn...'
              pip3 install --user uvicorn fastapi
            fi
            
            if ! python3 -c 'import flask' 2>/dev/null; then
              echo 'Устанавливаем Flask для OSMnx...'
              pip3 install --user flask flask-cors
            fi
            
            if ! python3 -c 'import osmnx' 2>/dev/null; then
              echo 'Устанавливаем OSMnx...'
              pip3 install --user osmnx geopandas
            fi
            
            echo '=== Проверяем модуль ==='
            cd backend
            python3 -c 'import app.simple_main; print(\"Модуль app.simple_main найден\")' || echo 'МОДУЛЬ НЕ НАЙДЕН'

            echo '=== Проверяем текущие процессы ==='
            CURRENT_USER=\$(whoami)
            echo \"Текущий пользователь: \$CURRENT_USER\"
            
            echo 'Процессы uvicorn:'
            ps aux | grep uvicorn | grep -v grep || true
            
            echo 'Процессы python (osmnx):'
            ps aux | grep 'backend_osmnx.py' | grep -v grep || true
            
            echo '=== Останавливаем старые процессы через PID файлы ==='
            # Останавливаем FastAPI если есть PID файл
            if [ -f /tmp/fastapi.pid ]; then
              OLD_PID=\$(cat /tmp/fastapi.pid)
              echo \"Найден PID файл FastAPI: \$OLD_PID\"
              if ps -p \$OLD_PID >/dev/null 2>&1; then
                echo \"Пытаемся остановить процесс \$OLD_PID\"
                kill \$OLD_PID 2>/dev/null && echo \"Процесс \$OLD_PID остановлен\" || echo \"Не удалось остановить процесс \$OLD_PID\"
                sleep 2
              else
                echo \"Процесс \$OLD_PID уже не существует\"
              fi
              rm -f /tmp/fastapi.pid
            else
              echo \"PID файл FastAPI не найден\"
            fi
            
            # Останавливаем OSMnx если есть PID файл
            if [ -f /tmp/osmnx.pid ]; then
              OLD_PID=\$(cat /tmp/osmnx.pid)
              echo \"Найден PID файл OSMnx: \$OLD_PID\"
              if ps -p \$OLD_PID >/dev/null 2>&1; then
                echo \"Пытаемся остановить процесс \$OLD_PID\"
                kill \$OLD_PID 2>/dev/null && echo \"Процесс \$OLD_PID остановлен\" || echo \"Не удалось остановить процесс \$OLD_PID\"
                sleep 2
              else
                echo \"Процесс \$OLD_PID уже не существует\"
              fi
              rm -f /tmp/osmnx.pid
            else
              echo \"PID файл OSMnx не найден\"
            fi
            
            sleep 2
            
            echo '=== Используем фиксированные порты ==='
            export FASTAPI_PORT=6002
            export OSMNX_PORT=6004
            
            echo \"Будем запускать FastAPI на порту \$FASTAPI_PORT\"
            echo \"Будем запускать OSMnx на порту \$OSMNX_PORT\"
            
            echo '=== Запускаем FastAPI ==='
            cd backend
            echo 'Текущая директория: \$(pwd)'
            
            nohup python3 -m uvicorn app.simple_main:app --host 0.0.0.0 --port \$FASTAPI_PORT > /tmp/fastapi.log 2>&1 &
            FASTAPI_PID=\$!
            echo \$FASTAPI_PID > /tmp/fastapi.pid
            echo \"FastAPI запущен с PID: \$FASTAPI_PID\"
            sleep 3
            
            echo '=== Запускаем OSMnx Backend ==='
            cd ..
            echo 'Текущая директория: \$(pwd)'
            echo 'Содержимое корневой директории: \$(ls -la)'
            
            # Модифицируем backend_osmnx.py для использования нужного порта
            sed -i \"s/port=5000/port=\$OSMNX_PORT/g\" backend_osmnx.py
            sed -i \"s/port=6004/port=\$OSMNX_PORT/g\" backend_osmnx.py
            sed -i \"s/port=[0-9]*/port=\$OSMNX_PORT/g\" backend_osmnx.py
            nohup python3 backend_osmnx.py > /tmp/osmnx.log 2>&1 &
            OSMNX_PID=\$!
            echo \$OSMNX_PID > /tmp/osmnx.pid
            echo \"OSMnx запущен с PID: \$OSMNX_PID\"
            sleep 3
            
            echo '=== Проверяем запуск сервисов ==='
            echo 'Процессы uvicorn:'
            ps aux | grep uvicorn | grep -v grep || echo 'uvicorn не найден'
            echo 'Процессы python (osmnx):'
            ps aux | grep 'backend_osmnx.py' | grep -v grep || echo 'osmnx не найден'
            
            echo '=== Проверяем FastAPI ==='
            curl -s http://localhost:\$FASTAPI_PORT/test || echo 'FastAPI test не отвечает'
            curl -s http://localhost:\$FASTAPI_PORT/health || echo 'FastAPI health не отвечает'
            
            echo '=== Проверяем OSMnx ==='
            curl -s http://localhost:\$OSMNX_PORT/api/health || echo 'OSMnx health не отвечает'
            
            echo '=== Проверяем файл backend_osmnx.py ==='
            if [ -f \"backend_osmnx.py\" ]; then
              echo \"Файл backend_osmnx.py найден\"
              echo \"Размер файла: \$(wc -l < backend_osmnx.py) строк\"
              echo \"Последние 5 строк файла:\"
              tail -5 backend_osmnx.py
            else
              echo \"Файл backend_osmnx.py НЕ НАЙДЕН\"
            fi
            
            echo '=== Логи FastAPI ==='
            cat /tmp/fastapi.log || echo 'FastAPI лог пуст'
            
            echo '=== Логи OSMnx ==='
            cat /tmp/osmnx.log || echo 'OSMnx лог пуст'
            
            echo \"Deployed FastAPI at: http://${{ secrets.VPS_HOST }}:\$FASTAPI_PORT/health\"
            echo \"Deployed OSMnx at: http://${{ secrets.VPS_HOST }}:\$OSMNX_PORT/api/health\"
          "